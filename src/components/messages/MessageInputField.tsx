import {
  MessageInputContainerStyle,
  MessageInputFieldStyle,
} from "../../utils/styles";

export const MessageInputField = () => {
  const sendMess = () => {};

  return (
    <MessageInputContainerStyle>
      <MessageInputFieldStyle />
    </MessageInputContainerStyle>
  );
};
